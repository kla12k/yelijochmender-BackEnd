// import 'dotenv/config';
// import { defineConfig } from 'drizzle-kit';
// import type { Config } from "drizzle-kit";

// export default defineConfig({
//   schema: './src/drizzle/schema.ts',
//   out: './migrations',
//   dialect: 'mysql',  // Use 'dialect' instead of 'driver'
//   dbCredentials: {
//     // Use a connection URL instead of separate properties
//     url: `mysql://${process.env.DB_USERNAME || 'root'}:${process.env.DB_PASSWORD || ''}@${process.env.DB_HOST || 'localhost'}:${process.env.DB_PORT || '3306'}/${process.env.DB_DATABASE || 'business_directory'}`,
//   },
// }) satisfies Config;
